/** Sections en gobernanzaModuloTipos (sin imports pesados — evita ciclos de módulo). */

/** Formularios publicados bajo TenantSuperAdmin / ParametrosGobernanza. */
export const GOBERNANZA_TIPO_SECTION_TENANT_SUPER_ADMIN = 'gobernanza';

/** Formularios publicados bajo TenantGlobal (operaciones rol tenantGlobal). */
export const GOBERNANZA_TIPO_SECTION_TENANT_GLOBAL = 'gobernanza-global';

/** Formularios publicados bajo ReglasTenant (Reglas SA). */
export const GOBERNANZA_TIPO_SECTION_REGLAS_SA = 'reglas';

/** Formularios publicados bajo ReglasTenantGlobal (reglas por tenant global concreto). */
export const GOBERNANZA_TIPO_SECTION_REGLAS_TG = 'reglas-tg';

/** Formularios publicados bajo PermisosGlobal (permisos SA). */
export const GOBERNANZA_TIPO_SECTION_PERMISOS_SA = 'permisos';

/** Formularios del hub ParametrizacionCorporativa (perfil, logos, documentos…). */
export const GOBERNANZA_TIPO_SECTION_EMPRESAS = 'empresas';
